const paramsUrlDef = new URLSearchParams({ page: 'home' })
export const paramsUrlDefStr = paramsUrlDef.toString()

const newUrlParam = new URLSearchParams({ page: 'admin' })
export const newUrlParamStr = newUrlParam.toString()